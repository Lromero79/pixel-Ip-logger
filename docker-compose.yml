version: '3.8'

services:
  ip-logger:
    build: .
    ports:
      - "8080:10000"
    volumes:
      - ./images:/app/images
      - ./access_logs.csv:/app/access_logs.csv
      - ./geo_cache.json:/app/geo_cache.json
    restart: unless-stopped
